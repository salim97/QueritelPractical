part of details_view;

class _DetailsMobile extends StatelessWidget {
  const _DetailsMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}